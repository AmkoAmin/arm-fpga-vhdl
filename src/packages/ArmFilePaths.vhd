package ArmFilePaths is

    --  Path to the test-vector directory (project-relative; adjust if needed).
    constant TESTVECTOR_FOLDER_PATH : STRING := "sim/data";

end package ArmFilePaths;

package body ArmFilePaths is
end package body ArmFilePaths;
